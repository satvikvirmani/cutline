export default function OfflinePage() {
  return (
    <main className="login-shell">
      <section className="login-card offline-card">
        <h1 className="login-title">Offline</h1>
        <p className="offline-copy">The app shell is available, but this page needs a network connection.</p>
      </section>
    </main>
  );
}

